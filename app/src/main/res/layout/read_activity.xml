<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0099cc">

    <ImageView
        android:id="@+id/page_preview_imgv"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <android.support.constraint.ConstraintLayout
        android:id="@+id/page_options_container"
        android:layout_width="100dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="@drawable/page_options_border_background"
        android:foregroundGravity="center_horizontal"
        android:padding="10dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            app:layout_constraintTop_toTopOf="parent">

            <Button
                android:id="@+id/exit_read_btn"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:text="out" />

            <Button
                android:id="@+id/fullscreen_read_btn"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:text="Fullecreen"
                android:textSize="9dp" />

            <Button
                android:id="@+id/continue_reading"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:layout_marginTop="20dp"
                android:text="Continue"
                android:textSize="11dp" />

        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="100dp"
            android:gravity="center"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="parent">

            <Button
                android:id="@+id/next_page_btn"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:text="NEXT" />

            <Button
                android:id="@+id/prev_page_btn"
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:text="back" />

        </LinearLayout>

    </android.support.constraint.ConstraintLayout>

</FrameLayout>